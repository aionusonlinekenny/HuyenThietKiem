-- Simple test version of upgrade_master.lua
-- If this works, then the original file has syntax issues

function main(NpcIndex)
    Talk(1, "", "Test: upgrade_master script loaded successfully!")
end

function UpgradeAttribTalk()
    Talk(1, "", "UpgradeAttribTalk function works!")
end
